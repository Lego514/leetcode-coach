/**
 * English key-insight hints, one per built-in problem. They point in a direction
 * without giving away the full solution. Written for this app; no LeetCode content.
 */
export const HINTS_EN: Record<number, string> = {
  // Arrays & Hashing
  217: 'How can you tell in O(1) whether you have already seen a number?',
  242: 'Two strings are anagrams exactly when every letter appears the same number of times in both.',
  1: 'At x, the number you need is target - x. Can you look up right away whether it appeared, and where?',
  383: 'Count the letters in magazine first, then check that ransomNote never needs more than that.',
  169: 'The majority element appears more than half the time. If different values cancel each other in pairs, which one survives? (Boyer-Moore voting)',
  409: 'Letters with an even count can all be used. How many letters with an odd count can you use, and how many unpaired letters fit in the middle?',
  14: 'Use the first string as a reference and compare the other strings character by character at each position.',
  49: 'Strings that are anagrams of each other produce the same key when sorted (or when turned into letter counts).',
  347: 'Count frequencies first. A frequency is at most n, so you can use buckets indexed by frequency, or a heap of size k.',
  271: 'A separator alone isn’t enough, because the strings may contain it. Try prefixing each string with its length.',
  238: 'Each answer is the product of everything to its left times everything to its right, and each side takes a single pass.',
  36: 'Keep one set per row, per column, and per 3×3 box; a box can be identified by (r // 3, c // 3).',
  128: 'Put the numbers in a set. x starts a consecutive run only when x - 1 isn’t in the set.',
  189: 'Reverse the whole array, then reverse the first k elements and the rest separately. Reduce k modulo n first.',
  525: 'Treat 0 as -1, and the problem becomes the longest subarray with sum 0. Record where each prefix sum first appears.',
  560: 'A subarray sum is prefix[j] - prefix[i]. At j, you need to know how many earlier prefix sums equal prefix[j] - k.',
  380: 'An array gives O(1) random access and a hash map gives O(1) lookup of positions. To delete, swap the target with the last element, then pop.',
  31: 'From the right, find the first position i that is smaller than its right neighbor, swap it with the rightmost larger value, then reverse everything after i.',
  362: 'Only the last 300 seconds matter. Keep timestamps in a queue and drop expired ones when you query.',
  41: 'The answer is always between 1 and n + 1. Can you place each value x at index x - 1, then find the first position that doesn’t match?',

  // Two Pointers
  125: 'Move one pointer from each end toward the middle, skipping non-alphanumeric characters and ignoring case.',
  283: 'Keep a pointer to where the next non-zero element belongs; after one pass, fill the remaining positions with zeros.',
  977: 'The largest square is always at one of the two ends. Compare the ends, moving inward, and fill the result from the back.',
  167: 'The array is sorted: if the sum is too small, move the left pointer; if it’s too large, move the right one.',
  15: 'Sort first, fix the first number, and the rest becomes Two Sum on the numbers to its right. Remember to skip duplicates.',
  16: 'Same as 3Sum: sort, fix one number, and use two pointers, tracking the sum closest to target as you go.',
  11: 'The area is limited by the shorter line. Moving the taller line can never help, so always move the shorter one.',
  75: 'Use three pointers to split the array into a 0 region, a 1 region, an unprocessed region, and a 2 region (Dutch national flag).',
  42: 'Water above a cell is min(highest on the left, highest on the right) - its height. With two pointers, process the side whose maximum is smaller.',

  // Sliding Window
  121: 'Track the lowest price seen so far, and each day compute the profit of selling today.',
  3: 'The window must not contain repeats. When a repeat appears, move the left edge just past that character’s previous position.',
  424: 'Window length minus the count of its most frequent letter is the number of replacements needed. Shrink when that exceeds k.',
  567: 'Slide a fixed window of length len(s1) across s2 and compare its letter counts with s1’s.',
  438: 'The same fixed-size window as Permutation in String, but record every start position that matches.',
  76: 'Grow the right edge until the window contains every required character, then shrink from the left as far as possible. Track how many kinds of characters are still missing.',
  239: 'Keep indices in a deque with decreasing values; the front is the window maximum. Pop expired indices from the front.',

  // Stack
  20: 'Push opening brackets. On a closing bracket, check that the top of the stack is its matching opener.',
  232: 'Use two stacks, one for pushing and one for popping. Only when the popping stack is empty, pour the other one into it.',
  844: 'Simulate from left to right: push ordinary characters and pop on #. For O(1) space, walk both strings from the right with two pointers.',
  155: 'With every push, also record the minimum so far, either as a pair or in a second stack.',
  150: 'Push numbers. On an operator, pop two numbers, compute, and push the result. Division truncates toward zero.',
  22: 'Backtrack: add an opening bracket while you have some left, and a closing bracket only while there are fewer closers than openers.',
  739: 'Keep a stack of indices with decreasing temperatures. When a warmer day arrives, pop the cooler days and compute their waits.',
  853: 'Process cars from closest to the target to farthest and compute each arrival time. A car that arrives no later than the fleet ahead joins that fleet.',
  394: 'On [, push the current string and repeat count. On ], pop them and combine.',
  735: 'A collision happens only when the stack top moves right and the new asteroid moves left. Use a while loop for chains of collisions.',
  227: 'Remember the previous operator as you read. For + and -, push the number (or its negative); for * and /, combine with the top of the stack. Sum the stack at the end.',
  84: 'For each bar, find the first shorter bar on each side; the area is height × width. An increasing stack finds them in one pass.',
  224: 'There are only +, -, and parentheses. On (, push the running result and sign; on ), pop them and merge.',
  895: 'Map each frequency to its own stack. Pop from the stack with the highest frequency.',
  32: 'Store indices on the stack, with the last unmatched position at the bottom. After each match, the length is the current index minus the stack top.',

  // Binary Search
  704: 'Compare the middle value within [lo, hi] and discard half each time.',
  278: 'You want the first bad version, which is the first position where the condition holds (a lower bound).',
  74: 'Treat the whole matrix as one sorted array; index i maps to (i // n, i % n).',
  875: 'Binary search on the speed: can speed k finish within h hours? That condition is monotonic.',
  153: 'Compare nums[mid] with nums[hi]: if mid is larger, the minimum is on the right; otherwise it’s on the left, including mid.',
  33: 'At each step, one half is always sorted. Figure out which half, then check whether target falls within its range.',
  981: 'Timestamps for a key are added in increasing order, so binary search that key’s list for the last timestamp ≤ the query.',
  528: 'Build prefix sums, pick a random number from 1 to the total, and binary search for the range it falls in.',
  658: 'The answer is a contiguous window of length k. Binary search on the window’s start by comparing x - arr[mid] with arr[mid + k] - x.',
  4: 'Binary search a split point in the shorter array so both halves have the same size and the left maximum is ≤ the right minimum.',

  // Linked List
  206: 'As you walk the list, point each node’s next at the previous node. Save the next node before changing the pointer.',
  21: 'Start from a dummy node and keep attaching the smaller head of the two lists.',
  141: 'Move a fast pointer two steps and a slow pointer one step. If there’s a cycle, they must meet.',
  876: 'Move a fast pointer two steps and a slow pointer one step; when the fast one reaches the end, the slow one is in the middle.',
  234: 'Find the middle, reverse the second half, then compare it with the first half node by node.',
  143: 'Three steps: find the middle, reverse the second half, then interleave the two halves.',
  19: 'Move one pointer n steps ahead, then move both together; when the front one reaches the end, the back one sits just before the node to remove. A dummy node handles removing the head.',
  138: 'First build a hash map from each original node to its copy, then wire up next and random on a second pass.',
  2: 'Add digit by digit starting from the ones place, and carry over, including a carry after the last digit.',
  287: 'Treat indices as nodes and nums[i] as next pointers; the duplicate is where the cycle begins (Floyd’s cycle detection).',
  146: 'A hash map finds a node in O(1), and a doubly linked list moves it to the front or removes the oldest node in O(1).',
  24: 'With a dummy node, handle one pair at a time: rewire three pointers, then advance two nodes.',
  328: 'Build separate chains for odd and even positions with two pointers, then attach the even chain after the odd one.',
  148: 'For O(n log n), split the list in half with slow and fast pointers, sort each half recursively, and merge (merge sort).',
  61: 'Find the length and connect the tail to the head to make a ring, then break it after the new tail (node n - k % n).',
  23: 'Keep the current head of each list in a min-heap and repeatedly attach the smallest; merging lists in pairs also works.',
  25: 'Check that k more nodes exist, reverse that group, and reconnect it. A dummy node and a pointer to the previous group’s tail make the joins easy.',

  // Trees
  226: 'Swap each node’s left and right subtrees, and recurse.',
  104: 'The depth of a tree is 1 + max(depth of left subtree, depth of right subtree).',
  543: 'The longest path through a node is left depth + right depth. Update a global maximum while computing depths.',
  110: 'Check the depth difference while computing depths. As soon as something is unbalanced, return a sentinel (such as -1) so callers stop early.',
  100: 'Recurse on both trees together: both empty means equal; one empty or different values means not equal.',
  101: 'Write a helper that checks whether two subtrees mirror each other: left’s left against right’s right, and left’s right against right’s left.',
  572: 'For each node in root, check whether the subtree rooted there equals subRoot (reuse Same Tree).',
  108: 'Use the middle element as the root, build the left subtree from the left half and the right subtree from the right half.',
  235: 'Use the BST property: if p and q are both smaller, go left; if both are larger, go right; otherwise the current node is the answer.',
  236: 'Have the recursion report whether p or q was found in a subtree. If both sides report one, the current node is the answer.',
  102: 'Use BFS. At the start of each round, note the queue length and process exactly that many nodes as one level.',
  103: 'Same as level-order traversal, but reverse every other level (or add to a deque from alternating ends).',
  199: 'In a level-order traversal, the last node of each level is the one visible from the right.',
  1448: 'During DFS, pass down the largest value on the path from the root. A node is good if it’s ≥ that value.',
  98: 'Every node has a valid range (lo, hi). Going left, the upper bound becomes the node’s value; going right, the lower bound does.',
  230: 'An in-order traversal of a BST visits values in increasing order, so the k-th visited node is the answer.',
  285: 'If p has a right subtree, the answer is its leftmost node. Otherwise, search from the root and remember the last node larger than p.',
  105: 'The first preorder value is the root. Its position in inorder tells you how many nodes are in the left subtree; a hash map of inorder indices makes that lookup fast.',
  113: 'During DFS, carry the current path and sum. At a leaf with a matching sum, add a copy of the path to the answer.',
  437: 'Use prefix sums as in Subarray Sum Equals K: during DFS, count the prefix sums on the path from the root, and decrement when leaving a node.',
  662: 'Use BFS and number the nodes (left child 2i, right child 2i + 1). A level’s width is rightmost number - leftmost number + 1.',
  863: 'Turn the tree into an undirected graph by recording each node’s parent, then BFS k steps from target.',
  124: 'Have each node return the best downward path sum using only one side (treat negatives as 0), and update a global answer with left + right + node.',
  297: 'Serialize with a preorder traversal and a marker for empty nodes; deserialize by reading values back in the same order recursively.',

  // Tries
  208: 'Each node keeps a dict of children and a flag marking whether a word ends there.',
  211: 'When the search hits “.”, try every child of the current node recursively.',
  212: 'Put all the words in a trie, then DFS from every cell, stopping as soon as the current path isn’t a prefix of any word.',
  336: 'For each split point of each word, check whether one side is a palindrome and whether the reverse of the other side exists (using a hash map or a trie).',
  588: 'Represent the directory structure as a trie; each node keeps its children and, for files, the file content.',

  // Heap / Priority Queue
  703: 'Keep a min-heap of size k; its top is the k-th largest value.',
  1046: 'Use a max-heap (negated values in Python). Smash the two largest stones and push back the difference if any remains.',
  973: 'Keep a max-heap of size k by distance, or simply sort all points by distance.',
  215: 'Keep a min-heap of size k. For average O(n), use Quickselect.',
  621: 'The most frequent task sets the frame: (max count - 1) × (n + 1) + the number of tasks sharing that max count, compared with the total number of tasks.',
  355: 'Store each user’s tweets with timestamps. To build a feed, push the newest tweet from each followed user onto a heap and take the top 10.',
  692: 'Count frequencies, then take the top k by higher frequency first and alphabetical order for ties. With a heap, be careful with the comparison.',
  295: 'Use two heaps: a max-heap for the smaller half and a min-heap for the larger half, keeping their sizes within one of each other.',
  632: 'Keep the current element of each list in a heap and track the current maximum. Repeatedly pop the smallest, replace it with the next element from its list, and update the range.',

  // Backtracking
  78: 'Each element is either taken or skipped; alternatively, enumerate forward using a start index.',
  39: 'The same number can be reused, so recurse with start still at i. Stop once the sum exceeds target.',
  40: 'Sort first. Each number can be used once (recurse with i + 1), and duplicates at the same level must be skipped.',
  46: 'Track which numbers are already on the path with a used array, and try every unused number at each level.',
  90: 'Sort first, and at the same level (i > start) skip a number equal to the previous one.',
  79: 'Start a DFS from every cell, temporarily mark visited cells, and restore them when backtracking.',
  131: 'From start, try every cut point, and recurse on the rest only when the prefix is a palindrome.',
  17: 'Each digit maps to a few letters. Choose one letter per digit in order, and record the path once its length matches the digits.',
  51: 'Place one queen per row, using three sets for occupied columns, diagonals (r - c), and anti-diagonals (r + c).',
  37: 'Find an empty cell, try each valid digit from 1 to 9, and recurse; if later steps fail, reset the cell to empty.',

  // Graphs
  733: 'Run DFS or BFS from the start, spreading only to cells with the start’s original color. If the new color is the same, return right away.',
  200: 'Scan every cell. Each unvisited land cell starts a new island; mark the whole island with DFS or BFS.',
  695: 'Same as Number of Islands, except the DFS returns the island’s area.',
  133: 'Use a hash map from original nodes to their copies, and check it before copying to avoid duplicates and infinite loops.',
  286: 'Multi-source BFS: put every gate in the queue at once and expand layer by layer; the first time you reach a room is its shortest distance.',
  994: 'Multi-source BFS: all rotten oranges start together, and each layer is one minute. At the end, check whether any fresh oranges remain.',
  542: 'Multi-source BFS: start from every 0 and expand outward to get each 1’s shortest distance.',
  417: 'Think in reverse: from the Pacific border, walk uphill; do the same from the Atlantic border. Cells reachable from both are the answer.',
  130: 'Run DFS from every O on the border to mark the ones that can’t be surrounded, then flip all other O’s to X.',
  207: 'Courses and prerequisites form a directed graph, and the question is whether it has a cycle. Use topological sort or three-color DFS.',
  210: 'Use topological sort (Kahn’s algorithm): take courses with in-degree 0 first, and that order is the answer. If some courses are left over, there’s a cycle.',
  310: 'Peel the graph like an onion, removing leaves (degree 1) layer by layer. The last one or two nodes are the answer.',
  261: 'A tree with n nodes has exactly n - 1 edges, is fully connected, and has no cycle. Union-Find can check this.',
  323: 'Union the endpoints of every edge with Union-Find, or DFS from each unvisited node, and count the groups.',
  684: 'Add edges one by one with Union-Find. The first edge whose endpoints are already in the same group is redundant.',
  721: 'Connect the emails within each account (with Union-Find or a graph), then output each group’s emails in sorted order.',
  1730: 'BFS from your position. The number of steps when you first reach food is the answer.',
  1197: 'BFS from the origin. Symmetry lets you consider only the first quadrant, but widen the bounds a little so needed detours aren’t cut off.',
  127: 'Each word is a node, connected to words that differ by one letter. BFS finds the fewest steps; patterns like h*t find neighbors quickly.',
  815: 'Run BFS over bus routes rather than stops; each layer means taking one more bus.',

  // Advanced Graphs
  1584: 'This is a minimum spanning tree: use Prim (repeatedly add the nearest point to the connected set) or Kruskal (sort edges and use Union-Find).',
  743: 'Run Dijkstra from k. The answer is the largest shortest distance, or -1 if some node can’t be reached.',
  787: 'With at most k stops, run k + 1 rounds of Bellman-Ford, each round updating from a copy of the previous round’s distances.',
  332: 'Sort each airport’s destinations and use Hierholzer’s algorithm: DFS, add an airport to the answer only after all its edges are used, then reverse.',
  778: 'You want the path whose highest cell is as low as possible: use a Dijkstra variant ordered by the maximum along the path, or binary search on time.',
  269: 'Compare adjacent words; the first differing letter gives an ordering. Build a graph and topologically sort it. Watch for the invalid case where a longer word comes before its own prefix.',

  // 1-D Dynamic Programming
  70: 'The ways to reach step n = the ways to reach step n - 1 + the ways to reach step n - 2.',
  746: 'dp[i] = cost[i] + min(dp[i - 1], dp[i - 2]); the answer is the smaller of the last two steps.',
  198: 'Each house is either robbed or skipped: dp[i] = max(dp[i - 1], dp[i - 2] + nums[i]).',
  213: 'Because the street is a circle, the first and last houses can’t both be robbed. Run House Robber once without the first house and once without the last.',
  5: 'Expand outward from every character (and every gap between two characters) to find the longest palindrome.',
  647: 'Expand from every center, as for the longest palindrome, and count one palindrome per successful expansion.',
  91: 'dp[i] depends on whether the last digit (1–9) and the last two digits (10–26) are valid codes. A 0 can’t be decoded on its own.',
  322: 'dp[a] = the fewest coins for amount a = min(dp[a - coin] + 1).',
  152: 'A negative number turns the maximum into the minimum, so track both the largest and the smallest product ending at each position.',
  139: 'dp[i] says whether the first i characters can be split. Look for some j < i with dp[j] true and s[j:i] in the dictionary.',
  300: 'dp[i] = the length of the longest increasing subsequence ending at nums[i]. For O(n log n), keep a tails array and binary search it.',
  416: 'The total must be even; then the question is whether some numbers add up to exactly half, which is a 0/1 knapsack.',
  377: 'This counts orderings (different orders count separately): dp[t] = sum(dp[t - num]), with the outer loop over target.',
  1235: 'Sort by end time. dp[i] = max(skip job i, take it plus the dp of the last job that doesn’t overlap), found with binary search.',

  // 2-D Dynamic Programming
  62: 'The number of ways into a cell = the cell above + the cell to the left.',
  1143: 'If the characters match, dp[i][j] = 1 + dp[i + 1][j + 1]; otherwise take the larger of dp[i + 1][j] and dp[i][j + 1].',
  309: 'Use three states: holding a stock, just sold (cooling down), and not holding but free to buy. Write each day’s transitions.',
  518: 'This counts combinations (order doesn’t matter): loop over coins outside and amounts inside to avoid double counting.',
  494: 'Memoize on (index, current sum), or turn it into a knapsack: pick numbers that add up to (sum + target) / 2.',
  97: 'dp[i][j] says whether the first i characters of s1 and the first j of s2 can form the first i + j of s3. If the lengths don’t add up, return False right away.',
  72: 'Matching characters need no operation; otherwise take the minimum of insert, delete, and replace, plus 1.',
  221: 'dp[i][j] is the side of the largest square with (i, j) as its bottom-right corner = min(top, left, top-left) + 1.',
  329: 'DFS from every cell for the longest increasing path and memoize. Paths strictly increase, so they can’t loop back, and no visited set is needed.',
  115: 'When the characters match, you can either use this character or skip it; when they don’t, you can only skip the character in s.',
  312: 'Think backwards: which balloon in a range is burst last? It splits the range into two independent subproblems.',
  10: 'dp[i][j] says whether the first i characters of s match the first j of p. For *, consider using it zero times and using it once more.',

  // Greedy
  53: 'Keep a running sum and drop it once it turns negative, starting fresh from the next element (Kadane).',
  55: 'Track the farthest position you can reach. If you step past it, the end is unreachable.',
  45: 'Think of jumps as BFS layers: within the current reach, find the farthest position the next jump can reach.',
  134: 'If total gas is less than total cost, it’s impossible. Otherwise, whenever the tank goes negative, restart from the next station.',
  846: 'Sort (or count), then repeatedly start from the smallest card and form a run of groupSize consecutive cards.',
  1899: 'Only consider triplets where no value exceeds the target, and check whether they can supply each of the target’s three values.',
  763: 'Record the last position of each letter. While scanning, keep extending the current part’s end, and cut when you reach it.',
  678: 'Track the lowest and highest possible count of unmatched opening brackets. * widens the range; fail if the highest goes negative, and never let the lowest drop below 0.',
  179: 'Use a custom sort: a goes before b when a + b > b + a as strings. Watch for the case where every number is 0.',

  // Intervals
  252: 'Sort by start time and check whether any meeting starts before the previous one ends.',
  57: 'Handle three parts: intervals entirely before the new one, intervals that overlap it (merge them), and intervals entirely after it.',
  56: 'Sort by start. When an interval starts at or before the previous interval’s end, merge them.',
  435: 'Sort by end time and keep intervals that end early; remove any interval that overlaps the last one kept.',
  253: 'Sort by start time and keep each room’s end time in a min-heap; the room that frees up first can be reused.',
  1851: 'Sort the intervals by start and the queries too. For each query, push intervals that start at or before it onto a heap ordered by length, then pop the ones that have already ended.',
  759: 'Put every employee’s intervals together, sort by start, and merge. The gaps between merged intervals are the common free time.',

  // Math & Geometry
  202: 'Keep replacing the number with the sum of the squares of its digits. If a number repeats, you’re in a cycle (fast and slow pointers work too).',
  66: 'Add one to the last digit and carry. If every digit is 9, the result needs an extra leading 1.',
  13: 'Read from left to right: if a symbol is smaller than the one to its right (like the I in IV), subtract it; otherwise add it.',
  9: 'Negative numbers are never palindromes. You can reverse just the second half of the digits and compare it with the first half.',
  48: 'Rotating clockwise = transposing (flipping over the main diagonal), then reversing each row.',
  54: 'Keep top, bottom, left, and right boundaries, and move a boundary inward after walking each side.',
  73: 'Use the first row and first column as markers, and separately remember whether they themselves need clearing, for O(1) space.',
  50: 'Fast exponentiation: x^n = (x^(n/2))^2, multiplying by x once more when n is odd. For negative n, invert x first.',
  43: 'Simulate long multiplication: num1[i] × num2[j] lands in positions i + j and i + j + 1.',
  2013: 'Count how many times each point appears. For a query, try every possible diagonal corner and multiply the counts of the other two corners.',
  8: 'Go step by step: skip leading spaces, read the sign, read digits, and clamp to the 32-bit range.',

  // Bit Manipulation
  136: 'a ^ a = 0 and a ^ 0 = a. XOR everything together and the pairs cancel out.',
  191: 'n & (n - 1) clears the lowest set bit; count how many times you can do that.',
  338: 'The number of 1 bits in i = the count for i >> 1, plus i’s lowest bit.',
  190: 'Take n’s lowest bit and append it to the result (shift the result left, shift n right), 32 times.',
  268: 'XOR every number from 0 to n with every number in the array; what’s left is the missing number (or subtract sums).',
  67: 'Add digit by digit from the ends of both strings, and carry over.',
  371: 'XOR is addition without carrying, and (a & b) << 1 is the carry. Repeat until there’s no carry. In Python, mask with 0xFFFFFFFF to handle negatives.',
  7: 'Pop digits off the end to build the reversed number, then check that it fits in the 32-bit range.',
};
